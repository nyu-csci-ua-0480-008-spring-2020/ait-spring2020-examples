fruits = ['apple', 'orange', 'dragon fruit']
fruits.forEach(console.log);
// higher order functions
// functions that have functions as arguments
// functions that returns functions (as return value)
// or func that takes func as arg and returns func
/*
for(let i = 0; i < 3; i++) {
  console.log(fruits[i]);
}
// USE of (not in) 
// element in an Array
// using in... every property in an object
// (enumerable property)
for(const f of fruits) {
  console.log(f);
}
const point = {x: 100, y: 200};
// go over every prop in object
for(const prop in point) {
  console.log(prop, point[prop]);
}
*/













